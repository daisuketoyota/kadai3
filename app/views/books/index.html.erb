<main>
        <p id="notice">Welcome! You have signed up successfully.</p>
        <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <img src=>
  <table class="table">
      <tbody>
        <tr>
         <th>image<th>
         </tr>
         <tr>
	       <th>name</th>
	       <th><%= @user.name %></th>
         </tr>
         <tr>
	       <th>introduction</th>
	       <th><%= @user.introduction %></th>
        </tr>
      </tbody>
</table>
<div class="row">
  <a class="btn btn-outline-secondary btn-block edit_user_id" href="/users/id/edit">
    <i class="fas fa-user-cog" aria-hidden="true"></i>
  </a>
</div>

  <h2>New Books</h2>
  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved: %>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>

  <%= form_with model: @book, method: :post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
	
  <h4>Body</h4>
  <%= f.text_area:body %>
	
	<h5>
  <%= f.submit 'Create Book' %>
  <% end %>
  <h5>
  </thead>
  <tbody>
  <h1>Books</h1>
  <table width='750'>
  <thead>
    <th width='200'>Title</th>
    <th width='400'>Body</th>
    <th colspan="3" width='150'></th>
  </thead>
  <tr>
  <% @books.each do |book| %>
  　<td><span><%= book.title %></span></td>
  　<td><span><%= book.body %></span></td>
  　<td><span><%= link_to 'Edit',edit_book_path(book.id)%></span></td>
  　<td><span><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></span></td>
  </tr>
  <% end %>
  </table>
  </tbody>
</table>
    </div>
  </div>
</div>

</main>